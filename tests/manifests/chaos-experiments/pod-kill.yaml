apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: pod-kill-payment-service
  namespace: default
spec:
  action: pod-kill
  mode: one
  selector:
    labelSelectors:
      app: payment-service
  duration: '5m'