apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: cpu-hog-ticket-service
  namespace: default
spec:
  mode: one
  selector:
    labelSelectors:
      app: ticket-service
  stressors:
    cpu:
      workers: 2
      load: 100
  duration: '5m'